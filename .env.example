# Everything MCP Server Configuration
# Everything HTTP Server URL (Defaults to auto-detecting port 8088 / 80 or Plugins.ini)
EVERYTHING_HTTP_URL=http://127.0.0.1:8088

# Optional: HTTP Server Authentication (if enabled in Everything HTTP Server plugin settings)
EVERYTHING_HTTP_USERNAME=
EVERYTHING_HTTP_PASSWORD=
